# ==============================================
# Product Catalog Microservice - Environment Variables
# ==============================================
# Copy this file to .env and update with your values

# Application Configuration
APP_NAME=product-ms
SERVER_PORT=8085

# Database Configuration - MySQL
# IMPORTANT: These credentials are used by BOTH:
#   1. docker-compose.yml (to CREATE the MySQL user)
#   2. Spring Boot application (to CONNECT to MySQL)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=product_catalog
DB_USERNAME=productuser
DB_PASSWORD=productpass

# JPA/Hibernate Configuration
JPA_DDL_AUTO=update
JPA_SHOW_SQL=true
JPA_FORMAT_SQL=true

# Swagger/OpenAPI Configuration
SWAGGER_ENABLED=true
API_DOCS_PATH=/api-docs
SWAGGER_UI_PATH=/swagger-ui.html

# ==============================================
# INSTRUCTIONS:
# ==============================================
# 1. Copy this file: cp env.example .env
# 2. Update the values in .env with your configuration
# 3. Never commit .env to git (already in .gitignore)
# 4. For production, use secure secret management (e.g., AWS Secrets Manager, Vault)
# ==============================================

